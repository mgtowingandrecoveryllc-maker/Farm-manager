import React from "react";
import ReactDOM from "react-dom/client";
import FarmManager from "./FarmManager.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FarmManager />
  </React.StrictMode>
);
